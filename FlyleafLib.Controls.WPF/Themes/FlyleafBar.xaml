<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                    mc:Ignorable="d"
                    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
                    xmlns:flwpf="clr-namespace:FlyleafLib.Controls.WPF">

    <Style x:Key="FlyleafBarDefault" TargetType="{x:Type flwpf:FlyleafBar}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate>
                    <ControlTemplate.Resources>
                        <ResourceDictionary>
                            <ResourceDictionary.MergedDictionaries>
                                <ResourceDictionary Source="pack://application:,,,/FlyleafLib.Controls.WPF;component/Resources/MaterialDesign.xaml"/>
                                <ResourceDictionary Source="pack://application:,,,/FlyleafLib.Controls.WPF;component/Resources/PopUpMenu.xaml"/>
                                <ResourceDictionary Source="pack://application:,,,/FlyleafLib.Controls.WPF;component/Resources/Slider.xaml"/>
                            </ResourceDictionary.MergedDictionaries>

                            <Storyboard x:Key="fadeIn" Timeline.DesiredFrameRate="10">
                                <DoubleAnimation BeginTime="0:0:0" Storyboard.TargetProperty="Opacity"  To="1" Duration="0:0:0.5" AutoReverse="False"/>
                            </Storyboard>

                            <Storyboard x:Key="fadeOut" Timeline.DesiredFrameRate="10">
                                <DoubleAnimation BeginTime="0:0:0" Storyboard.TargetProperty="Opacity"  To="0" Duration="0:0:2.0" AutoReverse="False"/>
                            </Storyboard>
                        </ResourceDictionary>
                    </ControlTemplate.Resources>
                    <Border DataContext="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type flwpf:FlyleafBar}}}" 
                            Background="#D4D0C8" VerticalAlignment="Bottom" Focusable="False" Margin="0"
                            SnapsToDevicePixels="True" UseLayoutRounding="True"
                            TextElement.Foreground="#000000" TextElement.FontSize="11" TextElement.FontFamily="Tahoma">
                        <Border.Style>
                            <Style TargetType="{x:Type Border}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Player.Activity.Mode}" Value="Idle">
                                        <DataTrigger.EnterActions>
                                            <RemoveStoryboard BeginStoryboardName="fadeInN" />
                                            <BeginStoryboard x:Name="fadeOutN" Storyboard="{StaticResource fadeOut}" />
                                        </DataTrigger.EnterActions>
                                        <Setter Property="IsHitTestVisible" Value="False"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding Player.Activity.Mode}" Value="Active">
                                        <DataTrigger.EnterActions>
                                            <RemoveStoryboard BeginStoryboardName="fadeInN" />
                                            <BeginStoryboard x:Name="fadeOutN2" Storyboard="{StaticResource fadeOut}" />
                                        </DataTrigger.EnterActions>
                                        <Setter Property="IsHitTestVisible" Value="False"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding Player.Activity.Mode, FallbackValue=FullActive, TargetNullValue=FullActive}" Value="FullActive">
                                        <DataTrigger.EnterActions>
                                            <RemoveStoryboard BeginStoryboardName="fadeOutN" />
                                            <RemoveStoryboard BeginStoryboardName="fadeOutN2" />
                                            <BeginStoryboard x:Name="fadeInN" Storyboard="{StaticResource fadeIn}" />
                                        </DataTrigger.EnterActions>
                                        <Setter Property="IsHitTestVisible" Value="True"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>
                        <Border BorderThickness="1,1,0,0" BorderBrush="White">
                            <Border BorderThickness="0,0,1,1" BorderBrush="#404040">
                                <Border BorderThickness="1,1,0,0" BorderBrush="#E0E0E0">
                                    <Border BorderThickness="0,0,1,1" BorderBrush="#808080">

                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="32"/>
                                <RowDefinition Height="44"/>
                            </Grid.RowDefinitions>

                            <Grid Grid.Row="0" Margin="5,6,5,2">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="auto"/>
                                </Grid.ColumnDefinitions>

                                <!--Current Time | Slider | Duration-->
                                <TextBlock Grid.Column="0" HorizontalAlignment="Left"   VerticalAlignment="Center" Text="{Binding Player.CurTime, Converter={StaticResource TicksToTime}}" d:Text="00:00:00"/>
                                <Slider    Grid.Column="1" IsSelectionRangeEnabled="True" SelectionStart="{Binding RelativeSource={RelativeSource Self}, Path=Value, Mode=OneWay}" Style="{DynamicResource FlyleafSlider}" Focusable="False" Margin="10,0,10,0" VerticalAlignment="Center" Value="{Binding Player.CurTime, Mode=TwoWay, Converter={StaticResource TicksToSeconds}}" Maximum="{Binding Player.Duration, Converter={StaticResource TicksToSeconds}}">
                                    <Slider.SelectionEnd>
                                        <MultiBinding Converter="{StaticResource SumConverter}">
                                            <Binding Path="SelectionStart" RelativeSource="{RelativeSource Self}" Mode="OneWay"/>
                                            <Binding Path="Player.BufferedDuration" Converter="{StaticResource TicksToSeconds}" Mode="OneWay"/>
                                        </MultiBinding>
                                    </Slider.SelectionEnd>
                                </Slider>
                                <TextBlock Grid.Column="2" HorizontalAlignment="Center" VerticalAlignment="Center" Text="{Binding Player.Duration, Converter={StaticResource TicksToTime}}" d:Text="00:00:00"/>
                            </Grid>

                            <Grid Grid.Row="1" Margin="5,0,5,2">
                                <Grid.Resources>
                                    <ResourceDictionary>
                                        <Style x:Key="IconButton" TargetType="Button">
                                            <Setter Property="Width" Value="24"/>
                                            <Setter Property="Height" Value="24"/>
                                            <Setter Property="Focusable" Value="False"/>
                                            <Setter Property="Background" Value="#D4D0C8"/>
                                            <Setter Property="BorderThickness" Value="2"/>
                                            <Setter Property="BorderBrush">
                                                <Setter.Value>
                                                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                                        <GradientStop Color="White" Offset="0"/>
                                                        <GradientStop Color="#404040" Offset="1"/>
                                                    </LinearGradientBrush>
                                                </Setter.Value>
                                            </Setter>
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="Button">
                                                        <Border Background="{TemplateBinding Background}" SnapsToDevicePixels="True">
                                                            <Grid>
                                                                <Border x:Name="OuterLight" BorderThickness="1,1,0,0" BorderBrush="White"/>
                                                                <Border x:Name="OuterDark" BorderThickness="0,0,1,1" BorderBrush="#404040"/>
                                                                <Border x:Name="InnerLight" BorderThickness="1,1,0,0" BorderBrush="#E0E0E0" Margin="1"/>
                                                                <Border x:Name="InnerDark" BorderThickness="0,0,1,1" BorderBrush="#808080" Margin="1"/>
                                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="2"/>
                                                            </Grid>
                                                        </Border>
                                                        <ControlTemplate.Triggers>
                                                            <Trigger Property="IsPressed" Value="True">
                                                                <Setter TargetName="OuterLight" Property="BorderBrush" Value="#404040"/>
                                                                <Setter TargetName="OuterDark" Property="BorderBrush" Value="White"/>
                                                                <Setter TargetName="InnerLight" Property="BorderBrush" Value="#808080"/>
                                                                <Setter TargetName="InnerDark" Property="BorderBrush" Value="#E0E0E0"/>
                                                            </Trigger>
                                                        </ControlTemplate.Triggers>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                            <Style.Triggers>
                                                <Trigger Property="IsPressed" Value="True">
                                                    <Setter Property="BorderBrush">
                                                        <Setter.Value>
                                                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                                                <GradientStop Color="#404040" Offset="0"/>
                                                                <GradientStop Color="White" Offset="1"/>
                                                            </LinearGradientBrush>
                                                        </Setter.Value>
                                                    </Setter>
                                                </Trigger>
                                            </Style.Triggers>
                                        </Style>
                                        <Style x:Key="PlayPauseButton" TargetType="Button" BasedOn="{StaticResource IconButton}">
                                            <Setter Property="Width" Value="34"/>
                                            <Setter Property="Height" Value="28"/>
                                            <Setter Property="Background" Value="#D4D0C8"/>
                                            <Setter Property="Foreground" Value="Black"/>
                                        </Style>
                                    </ResourceDictionary>
                                </Grid.Resources>

                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="auto"/>
                                    <ColumnDefinition Width="auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="auto"/>
                                    <ColumnDefinition Width="auto"/>
                                    <ColumnDefinition Width="auto"/>
                                    <ColumnDefinition Width="auto"/>
                                    <ColumnDefinition Width="auto"/>
                                    <ColumnDefinition Width="auto"/>
                                </Grid.ColumnDefinitions>

                                <!--Play / Pause Button-->
                                <Button Grid.Column="0" Command="{Binding Player.Commands.TogglePlayPause}" VerticalAlignment="Center">
                                    <Button.Style>
                                        <Style TargetType="Button" BasedOn="{StaticResource PlayPauseButton}">
                                            <Setter Property="Content" Value="{materialDesign:PackIcon Kind=Play, Size=20}"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Player.Status}" Value="Playing">
                                                    <Setter Property="Content" Value="{materialDesign:PackIcon Kind=Pause, Size=20}"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                </Button>

                                <!--Subtitles Button-->
                                <Button x:Name="PART_ContextMenuOwner_Subtitles" Command="{Binding OpenContextMenu}" CommandParameter="{Binding RelativeSource={RelativeSource Self}}" Grid.Column="1" VerticalAlignment="Center" Margin="2,0,0,0">
                                    <Button.Style>
                                        <Style TargetType="Button" BasedOn="{StaticResource IconButton}">
                                            <Setter Property="Content" Value="{materialDesign:PackIcon Kind=CcOutline, Size=16}"/>
                                            <Setter Property="ContextMenu">
                                                <Setter.Value>
                                                    <ContextMenu ItemsSource="{StaticResource MenuSubtitlesStreams}" Style="{StaticResource FlyleafContextMenu}"/>
                                                </Setter.Value>
                                            </Setter>
                                            <Style.Triggers>
                                                <EventTrigger RoutedEvent="Click">
                                                    <EventTrigger.Actions>
                                                        <BeginStoryboard>
                                                            <Storyboard>
                                                                <BooleanAnimationUsingKeyFrames Storyboard.TargetProperty="ContextMenu.IsOpen">
                                                                    <DiscreteBooleanKeyFrame KeyTime="0:0:0" Value="True"/>
                                                                </BooleanAnimationUsingKeyFrames>
                                                            </Storyboard>
                                                        </BeginStoryboard>
                                                    </EventTrigger.Actions>
                                                </EventTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                </Button>

                                <!--Quality + Movies Title-->
                                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center" Margin="5,0,0,0">
                                    <Button x:Name="PART_ContextMenuOwner_Video" Command="{Binding OpenContextMenu}" CommandParameter="{Binding RelativeSource={RelativeSource Self}}">
                                        <Button.Style>
                                            <Style TargetType="Button" BasedOn="{StaticResource IconButton}">
                                                <Setter Property="Content" Value="{materialDesign:PackIcon Kind=QualityLow, Size=16}"/>
                                                <Setter Property="ContextMenu">
                                                    <Setter.Value>
                                                        <ContextMenu ItemsSource="{StaticResource MenuVideoStreams}" Style="{StaticResource FlyleafContextMenu}"/>
                                                    </Setter.Value>
                                                </Setter>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Player.Video.Height, Converter={StaticResource QualityToLevels}}" Value="Low">
                                                        <Setter Property="Content" Value="{materialDesign:PackIcon Kind=QualityLow, Size=16}"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Player.Video.Height, Converter={StaticResource QualityToLevels}}" Value="Med">
                                                        <Setter Property="Content" Value="{materialDesign:PackIcon Kind=QualityMedium, Size=16}"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Player.Video.Height, Converter={StaticResource QualityToLevels}}" Value="High">
                                                        <Setter Property="Content" Value="{materialDesign:PackIcon Kind=QualityHigh, Size=16}"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Player.Video.Height, Converter={StaticResource QualityToLevels}}" Value="_4K">
                                                        <Setter Property="Content" Value="{materialDesign:PackIcon Kind=Video4kBox, Size=16}"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Button.Style>
                                    </Button>
                                    <materialDesign:PackIcon Kind="VideoInputAntenna" Height="16" Width="16" Foreground="Black" VerticalAlignment="Center" Margin="5,0,0,0" Visibility="{Binding Player.IsLive, Converter={StaticResource BooleanToVisibility}, FallbackValue=Collapsed, TargetNullValue=Collapsed}"/>
                                    <TextBlock VerticalAlignment="Center" Margin="5,0,0,0" Text="{Binding Player.Playlist.Selected.Title}" MaxWidth="200" TextTrimming="CharacterEllipsis"/>
                                </StackPanel>

                                <Button Grid.Column="3" VerticalAlignment="Center" Content="{materialDesign:PackIcon Kind=SkipPrevious, Size=16}" Command="{Binding Player.Commands.Reopen}" CommandParameter="{Binding Player.Playlist.PrevItem}">
                                    <Button.Style>
                                        <Style TargetType="Button" BasedOn="{StaticResource IconButton}">
                                            <Setter Property="Visibility" Value="Visible"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Player.Playlist.PrevItem}" Value="{x:Null}">
                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                </Button>

                                <Button Grid.Column="4" VerticalAlignment="Center" Content="{materialDesign:PackIcon Kind=SkipNext, Size=16}" Command="{Binding Player.Commands.Reopen}" CommandParameter="{Binding Player.Playlist.NextItem}">
                                    <Button.Style>
                                        <Style TargetType="Button" BasedOn="{StaticResource IconButton}">
                                            <Setter Property="Visibility" Value="Visible"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Player.Playlist.NextItem}" Value="{x:Null}">
                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                </Button>

                                <!--Volume Button-->
                                <Button Grid.Column="5" VerticalAlignment="Center" Command="{Binding Player.Commands.ToggleMute}">
                                    <Button.Style>
                                        <Style TargetType="Button" BasedOn="{StaticResource IconButton}">
                                            <Setter Property="Content" Value="{materialDesign:PackIcon Kind=VolumeHigh, Size=16}"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Player.Audio.Volume, Converter={StaticResource VolumeToLevels}}" Value="Mute">
                                                    <Setter Property="Content" Value="{materialDesign:PackIcon Kind=VolumeMute, Size=16}"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Player.Audio.Volume, Converter={StaticResource VolumeToLevels}}" Value="Low">
                                                    <Setter Property="Content" Value="{materialDesign:PackIcon Kind=VolumeLow, Size=16}"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Player.Audio.Volume, Converter={StaticResource VolumeToLevels}}" Value="Med">
                                                    <Setter Property="Content" Value="{materialDesign:PackIcon Kind=VolumeMedium, Size=16}"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Player.Audio.Mute}" Value="True">
                                                    <Setter Property="Content" Value="{materialDesign:PackIcon Kind=VolumeMute, Size=16}"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                </Button>

                                <!--Volume Slider-->
                                <Slider Grid.Column="6" Style="{DynamicResource FlyleafSlider}" Width="80" VerticalAlignment="Center" Margin="5,0,5,0" Minimum="0" Maximum="{Binding Player.Config.Audio.VolumeMax, Mode=OneWay}" Value="{Binding Player.Audio.Volume}"/>

                                <!--Open Settings Button-->
                                <Button Grid.Column="7" VerticalAlignment="Center" Style="{StaticResource IconButton}" Content="{materialDesign:PackIcon Kind=SettingsOutline, Size=16}" Command="{Binding OpenSettingsCmd}"/>

                                <!--Toggle Fullscreen Button-->
                                <ToggleButton Grid.Column="8" VerticalAlignment="Center" Width="24" Height="24" Background="Transparent" BorderThickness="0"
                                              IsChecked="{Binding Player.Host.IsFullScreen}"
                                              Content="{materialDesign:PackIcon Kind=Fullscreen, Size=20}"
                                              materialDesign:ToggleButtonAssist.OnContent="{materialDesign:PackIcon Kind=FullscreenExit, Size=20}"/>
                            </Grid>
                        </Grid>
                                    </Border>
                                </Border>
                            </Border>
                        </Border>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
